<div class="container waylon landing">
    <p>The following views are available on this instance:</p>
    <ul>
        <% get_views().each do |view| %>
            <li><a href="/view/<%= view %>"><%= view %></a></li>
    <% end %>
    </ul>
</div>
